[pytest]
testpaths = tests
markers =
    pg_eval: PostgreSQL evaluation harness tests (requires EVAL_DATABASE_URL)
    monday_live: Real Monday.com API tests (requires RUN_MONDAY_LIVE_TESTS=yes and MONDAY_API_KEY; live test count currently 0)
