## Summary
- Treat Gmail `connection_config.user_id` as an API selector (`me` is valid) and verify account identity via profile `email_address` against a normalized recipient allowlist.
- Fail closed on unknown selectors and literal email mismatches without changing OAuth seeding or token resolution.
- Extend readiness tests and document selector vs identity semantics in 10f.

## Test plan
- [x] readiness tests
- [x] workflow contract tests
- [x] hermetic live-eval tests
- [x] release gate CI contract tests
- [ ] Release Gate CI on PR
