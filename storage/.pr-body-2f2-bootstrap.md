## Summary
- Add PostgreSQL health check, schema bootstrap, and strict integration_db gate (collect 7, JUnit 7/7) to the live-eval `foundation` job.
- Step-scope `ENV`/`DATABASE_URL` to database steps only; hermetic/offline steps no longer inherit `DATABASE_URL`.
- Extend workflow contract tests to lock bootstrap order, JUnit gate, and foundation env boundaries.

## Test plan
- [x] workflow contract tests
- [x] hermetic live-eval tests
- [x] release gate CI contract tests
- [x] schema migration tests
- [ ] Release Gate CI on PR
